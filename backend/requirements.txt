fastapi==0.110.1
uvicorn==0.25.0
starlette==0.37.2
motor==3.3.1
pymongo==4.5.0
dnspython==2.8.0
python-dotenv==1.2.1
bcrypt==4.1.3
PyJWT==2.11.0
python-jose==3.5.0
pydantic==2.12.5
pydantic[email]
email-validator==2.3.0
python-multipart==0.0.22
reportlab==4.4.10
anthropic==0.105.2
openai==2.44.0
httpx==0.28.1
anyio==4.12.1
passlib==1.7.4
stripe==14.4.0
pycryptodome==3.20.0
# La usa food_semantic (embeddings de los alimentos) y por tanto TODO el asistente:
# agent_tools la importa arriba del fichero, así que sin numpy el chat no arranca. Estaba
# instalada en el venv local y no declarada aquí, y al desplegar el asistente-agente el
# 08-08-2026 el pod se quedó sin ella: `import agent_tools` -> ModuleNotFoundError.
#
# 2.4.6 y no la 2.5.1 que hay en local: el contenedor va con Python 3.11 y de la 2.5 en
# adelante numpy pide 3.12. Si algún día se sube el Python del Dockerfile, esto se puede
# subir con él.
numpy==2.4.6
